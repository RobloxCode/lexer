#include <stdio.h>

int some_fn(void *some_param) {
    puts("do some code");
    int var = 134 + 12345;
    // int arr[10] = {0};
    const float PI = 3.1415;
    return 0;

    if (var != 100) {
        return 0;
    }

    if (var <= -3) {
        return 0;
    }

    float dec = 13.0.3f;
    float dec2 = 2.71;
    int number = 1;

    /*
     * this is a comment
     */

    var++;
    //
}
